---
description: GitHub Flow・ブランチ命名・push 禁止物
globs: 
---

# Git ルール

- **GitHub Flow**: GitHub Flow を標準とする。main ブランチへの直接コミットは禁止。
- **ブランチ命名**: GitHub の規約に従う（feature/*, fix/*, chore/* 等）。
- **PR マージ**: PR の承認・マージは必ず人間が行う（自動マージ禁止）。
- **push 禁止**: シークレット、鍵ファイル、大容量バイナリを push しない。
